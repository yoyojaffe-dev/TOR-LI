# No Python dependencies. This file exists so Nixpacks detects the Python
# provider and provisions a runtime; the site is static and served by
# `python3 -m http.server $PORT` (see railway.json startCommand).
